package com.tools.commons.compress;

import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;

import org.apache.commons.compress.archivers.zip.ZipArchiveEntry;
import org.apache.commons.compress.archivers.zip.ZipArchiveOutputStream;

public class Compress {
	public static void main(String[] args) throws IOException {
		
		//压缩
//		  ZipArchiveEntry entry = new ZipArchiveEntry("CompressTest");  
//	      //要压缩的文件  
//	      File f=new File("d:\\test1\\ZFYK.cfg");  
//	      FileInputStream fis=new FileInputStream(f);  
//	      //输出的对象 压缩的文件  
//	      ZipArchiveOutputStream zipOutput=new ZipArchiveOutputStream(new File("d:\\test1\\ZFYK.tar"));    
//	      zipOutput.putArchiveEntry(entry);  
//	      int i=0,j;  
//	      while((j=fis.read()) != -1)  
//	      {   
//	       zipOutput.write(j);  
//	       i++;  
//	       System.out.println(i);  
//	      }  
//	      zipOutput.closeArchiveEntry();  
//	      zipOutput.close();  
//	      fis.close();		  
		
		//
	}
}
